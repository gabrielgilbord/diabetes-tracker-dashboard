{
  "name": "diabetes-tracker-monorepo",
  "version": "1.0.0",
  "description": "Diabetes Tracker - Monorepo con Dashboard Next.js",
  "scripts": {
    "build": "cd dashboard && npm run build",
    "dev": "cd dashboard && npm run dev",
    "start": "cd dashboard && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}